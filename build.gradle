task cleanAll(type: GradleBuild) {
    tasks = ['chat:clean']
}

task linkAll(type: GradleBuild) {
    tasks = ['chat:linkClient', 'chat:linkServer']
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.2.1'
}
