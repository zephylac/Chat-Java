task cleanAll(type: GradleBuild) {
    tasks = ['chat:clean', 'server:clean']
}

task linkAll(type: GradleBuild) {
    tasks = ['chat:link', 'server:link']
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.2.1'
}
